#include "main.h"

/**
 * _abs -function that returns the absolute value of an integer
 * @num: integer to find the absolute value of
 * Return: absolute value of num
 */

int _abs(int num)
{
    return (num < 0) ? -num : num;
}
